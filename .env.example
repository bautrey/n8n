# n8n Workflow Automation - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================
# Database name for n8n data storage
POSTGRES_DB=n8n

# Database user (avoid using 'postgres' for production)
POSTGRES_USER=n8n_user

# Database password (generate a strong password for production)
POSTGRES_PASSWORD=change_this_password

# ============================================================================
# n8n Authentication
# ============================================================================
# Basic authentication username for n8n UI
N8N_BASIC_AUTH_USER=admin

# Basic authentication password for n8n UI (use a strong password)
N8N_BASIC_AUTH_PASSWORD=change_this_password

# ============================================================================
# n8n Encryption & Security
# ============================================================================
# Encryption key for storing credentials securely
# IMPORTANT: Generate a random 32-character string
# You can generate one with: openssl rand -hex 16
# Or: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
# Or: python3 -c "import secrets; print(secrets.token_hex(16))"
N8N_ENCRYPTION_KEY=your_32_character_encryption_key_here

# ============================================================================
# n8n API Access
# ============================================================================
# API key for REST API access
# IMPORTANT: Create this in n8n UI at Settings > n8n API > Create an API key
# Leave blank until you create the key in the UI after first login
N8N_API_KEY=

# ============================================================================
# n8n Network Configuration (Optional)
# ============================================================================
# Host for n8n (default: localhost)
N8N_HOST=localhost

# Protocol for n8n (default: http)
N8N_PROTOCOL=http

# Webhook base URL (default: http://localhost:5678/)
WEBHOOK_URL=http://localhost:5678/
